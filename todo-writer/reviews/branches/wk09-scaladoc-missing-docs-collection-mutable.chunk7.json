{"verdict":"revise","summary":"The added Scaladoc contains an overbroad default-preservation claim, an inaccurate protected-method contract, an off-by-one growth threshold, and an unresolved NEEDS-HUMAN marker.","items":[{"file":"library/src/scala/collection/mutable/Map.scala","symbol":"Map.WithDefault","line":292,"severity":"blocker","issue":"The class documentation says that transformer methods other than `concat`, `empty`, and `clone` do not preserve the default. This wrapper's `fromSpecific` and `newSpecificBuilder` explicitly create `WithDefault` instances, so same-type transformations such as `filter` preserve it.","suggestion":"Limit the claim to transformations that use `mapFactory` (such as `map`), or enumerate the operations whose results lose the default.","confidence":"high","needs_human":false},{"file":"library/src/scala/collection/mutable/OpenHashMap.scala","symbol":"OpenHashMap.put","line":228,"severity":"blocker","issue":"The documentation says the table grows when more than half its slots are occupied or deleted, but the pre-insertion condition `2 * (size + deleted) > mask` grows an eight-slot table when exactly four slots are occupied/deleted and another entry is being added.","suggestion":"Say that growth occurs when adding would exceed the implementation's load threshold, or correct the threshold wording to describe the actual condition.","confidence":"high","needs_human":false},{"file":"library/src/scala/collection/mutable/OpenHashMap.scala","symbol":"OpenHashMap.OpenHashMapIterator.next","line":340,"severity":"blocker","issue":"The added `@note NEEDS-HUMAN` leaves an unresolved question in Scaladoc: after exhaustion, `next()` indexes `table(index)` and throws `ArrayIndexOutOfBoundsException`, rather than the required `NoSuchElementException`.","suggestion":"Human decision needed: fix `next()` to enforce the `Iterator.next()` contract, then document that exception normally; do not leave the NEEDS-HUMAN marker in published documentation.","confidence":"high","needs_human":true},{"file":"library/src/scala/collection/mutable/PriorityQueue.scala","symbol":"PriorityQueue.fixUp","line":173,"severity":"blocker","issue":"The comment promises to restore the heap invariant in the supplied `as` array, but the body compares values in `as` while swapping entries in `resarr.p_array`. It only has the described effect when `as` is that backing array.","suggestion":"Either fix `fixUp` to swap in `as`, or document and enforce that `as` must be this queue's backing array.","confidence":"high","needs_human":false}],"bonus_findings":[]}