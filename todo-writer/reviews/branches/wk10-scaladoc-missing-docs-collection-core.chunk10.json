{"verdict":"revise","summary":"Several iterator-view comments omit or misstate behavior for valid boundary arguments and flipped filters.","items":[{"file":"library/src/scala/collection/View.scala","symbol":"View.Filter.isEmpty","line":209,"severity":"blocker","issue":"The comment says emptiness means no element passes `p`, but `isFlipped = true` retains elements that fail `p`; in that case it is empty when every element passes `p`.","suggestion":"Describe emptiness in terms of no element being selected by `p` and `isFlipped`, or explicitly cover both polarities.","confidence":"high","needs_human":false},{"file":"library/src/scala/collection/View.scala","symbol":"View.DropRight","line":347,"severity":"blocker","issue":"The iterator and `isEmpty` comments claim the unknown-size path always uses an `n`-element lookahead and reads up to `n + 1` elements. For `n <= 0`, `dropRightIterator` returns the underlying iterator directly, so neither claim holds.","suggestion":"Qualify the buffering and read-ahead behavior with `n > 0`; document that non-positive `n` drops nothing.","confidence":"high","needs_human":false},{"file":"library/src/scala/collection/View.scala","symbol":"View.TakeRight","line":416,"severity":"blocker","issue":"The comments say an unknown-size underlying iterator is fully consumed on its first query. `takeRightIterator` instead returns an empty iterator for `n <= 0` and returns the underlying iterator unchanged for `n == Int.MaxValue`, neither of which consumes it fully.","suggestion":"Limit the full-consumption statement to `0 < n && n < Int.MaxValue`, and document the non-positive and `Int.MaxValue` cases.","confidence":"high","needs_human":false},{"file":"library/src/scala/collection/View.scala","symbol":"View.Patched.iterator","line":678,"severity":"blocker","issue":"The comment says replacement starts at `from` and drops `replaced` elements, but delegated `Iterator.patch` clamps a negative `from` to 0 and treats a non-positive `replaced` count as dropping none.","suggestion":"Document the normalization of negative `from` and non-positive `replaced`, or avoid wording that asserts the unnormalized values are applied literally.","confidence":"high","needs_human":false},{"file":"library/src/scala/collection/View.scala","symbol":"View.ScanLeft","line":472,"severity":"ok","issue":"","suggestion":"","confidence":"high","needs_human":false}],"bonus_findings":[{"file":"library/src/scala/collection/View.scala","symbol":"View.Filter.apply","line":229,"issue":"Pre-existing filter fusion changes semantics for two nested flipped filters: stacking retains `!p1 && !p2`, while the fused `Filter(..., p1 && p2, true)` retains `!p1 || !p2`.","suggestion":"Fuse flipped filters with a disjunction, or only fuse non-flipped filters."}]}