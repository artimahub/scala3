```scala
library/src/scala/util/Try.scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
=======
  /** Maps the given function to the value from this `Try` if it satisfies the predicate `p`, otherwise returns this if this is a `Failure`.

   *  @tparam U the type of the mapped value
   *  @param f the function to apply to the value if this is a `Success` and satisfies `p`
   *  @return a `Success` containing the result of applying `f` to the value if this is a `Success` and satisfies `p`, or this `Failure` unchanged; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
>>>>>>> REPLACE
```

```scala
library/src/scala/util/Try.scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
=======
  /** Returns the given function applied to the value from this `Try` if it satisfies the predicate `p`, otherwise returns this if this is a `Failure`.

   *  @tparam U the type of the value in the resulting `Try`
   *  @param f the function to apply to the value if this is a `Success` and satisfies `p`
   *  @return the `Try` returned by `f` applied to the value if this is a `Success` and satisfies `p`, or this `Failure` unchanged; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
>>>>>>> REPLACE
```

```scala
library/src/scala/util/Try.scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   */
  def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
=======
  /** Applies the given function `f` if this is a `Success` and satisfies the predicate `p`, otherwise returns `Unit` if this is a `Failure`.

   *  @tparam U the (discarded) result type of the function `f`
   *  @param f the function to apply to the value if this is a `Success` and satisfies `p`
   */
  def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
>>>>>>> REPLACE
```

```scala
library/src/scala/util/Try.scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @param q TODO FILL IN
   *  @return TODO FILL IN
   */
  def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
=======
  /** Creates a non-strict filter, which eventually converts this to a `Failure` if the predicate `q` is not satisfied.

   *  @param q the predicate used to test elements
   *  @return an object of class `WithFilter`, which supports `map`, `flatMap`, `foreach`, and `withFilter` operations. All these operations apply to those elements of this `Try` which satisfy the predicate `q`.
   */
  def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
>>>>>>> REPLACE
```