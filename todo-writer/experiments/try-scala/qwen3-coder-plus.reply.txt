library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
   */
  final class WithFilter(p: T => Boolean) uses Try.this {
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
    def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
    def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     */
    def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
    /** TODO FILL IN
     *
     *  @param q TODO FILL IN
     *  @return TODO FILL IN
     */
    def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
  }
=======
   */
  final class WithFilter(p: T => Boolean) uses Try.this {
    /** Maps the given function to the value from this `Try` if it satisfies the predicate.
     *
     *  @tparam U the type of the mapped value
     *  @param f the function to apply to the value if it satisfies the predicate
     *  @return a `Success` containing the result of applying `f` to the value if this is a `Success` that satisfies the predicate, otherwise this `Failure` unchanged; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
     */
    def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
    /** Returns the given function applied to the value from this `Try` if it satisfies the predicate.
     *
     *  @tparam U the type of the value in the resulting `Try`
     *  @param f the function to apply to the value if it satisfies the predicate
     *  @return the `Try` returned by `f` applied to the value if this is a `Success` that satisfies the predicate, otherwise this `Failure` unchanged; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
     */
    def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
    /** Applies the given function to the value from this `Try` if it satisfies the predicate.
     *
     *  @tparam U the (discarded) result type of the function `f`
     *  @param f the function to apply to the value if it satisfies the predicate
     */
    def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
    /** Creates a new `WithFilter` with an additional condition.
     *
     *  @param q the additional predicate to test the value against
     *  @return a `WithFilter` that combines both predicates
     */
    def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
  }
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
}

/** TODO FILL IN
 *
 *  @tparam T TODO FILL IN
 *  @param exception TODO FILL IN
 */
final case class Failure[+T](exception: Throwable) extends Try[T] { self: Failure[T]^ =>
  /** TODO FILL IN */
  override def isFailure: Boolean = true
  /** TODO FILL IN */
  override def isSuccess: Boolean = false
  /** TODO FILL IN */
  override def get: T = throw exception
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def getOrElse[U >: T](default: => U): U = default
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U]^{default} =
    try default catch { case NonFatal(e) => Failure(e) }
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{this} = this.asTryOf[U]
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param ev TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U]^{this} = this.asTryOf[U]
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   */
  override def foreach[U](f: T => U): Unit = ()
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param s TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{f} =
    try f(exception) catch { case NonFatal(e) => Failure(e) }
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def map[U](f: T => U): Try[U]^{this} = this.asTryOf[U]
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{this} = this.asTryOf[U]
  /** TODO FILL IN
   *
   *  @param p TODO FILL IN
   *  @return TODO FILL IN
   */
  override def filter(p: T => Boolean): Try[T]^{this} = this
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U]^{this, pf.only[Control]} = {
    val marker = Statics.pfMarker
    try {
=======
}

/** Represents a failed computation that holds an exception.
 *
 *  @tparam T the type of the value that would have been computed by a successful `Try`
 *  @param exception the exception that caused the failure
 */
final case class Failure[+T](exception: Throwable) extends Try[T] { self: Failure[T]^ =>
  /** Returns `true` since this is a `Failure`. */
  override def isFailure: Boolean = true
  /** Returns `false` since this is a `Failure`. */
  override def isSuccess: Boolean = false
  /** Throws the exception held by this `Failure`. */
  override def get: T = throw exception
  /** Returns the given `default` argument since this `Try` is a `Failure`.
   *
   *  @tparam U the type of the returned value, a supertype of `T`
   *  @param default the default value to return if this is a `Failure`
   *  @return the `default` value
   */
  override def getOrElse[U >: T](default: => U): U = default
  /** Returns the given `default` argument since this `Try` is a `Failure`.
   *
   *  @tparam U the type of the value in the returned `Try`, a supertype of `T`
   *  @param default the fallback `Try` to return if this is a `Failure` (evaluated lazily)
   *  @return the `default` `Try`; any non-fatal exception thrown while evaluating `default` is caught and returned as a `Failure`
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U]^{default} =
    try default catch { case NonFatal(e) => Failure(e) }
  /** Returns this `Failure` unchanged since there is no value to apply the function to.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param f the function to apply to the value if this were a `Success`
   *  @return this `Failure` unchanged
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{this} = this.asTryOf[U]
  /** Returns this `Failure` unchanged since there is no value to flatten.
   *
   *  @tparam U the type of the value in the inner `Try`
   *  @param ev evidence that `T` is itself a `Try[U]`
   *  @return this `Failure` unchanged
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U]^{this} = this.asTryOf[U]
  /** Does nothing since this is a `Failure`.
   *
   *  @tparam U the (discarded) result type of the function `f`
   *  @param f the function to apply to the value if this were a `Success`
   */
  override def foreach[U](f: T => U): Unit = ()
  /** Applies the given function to the exception held by this `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param s the function to apply if this were a `Success`
   *  @param f the function to apply to the exception if this is a `Failure`
   *  @return the `Try` returned by applying `f` to the exception; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{f} =
    try f(exception) catch { case NonFatal(e) => Failure(e) }
  /** Returns this `Failure` unchanged since there is no value to map.
   *
   *  @tparam U the type of the mapped value
   *  @param f the function to apply to the value if this were a `Success`
   *  @return this `Failure` unchanged
   */
  override def map[U](f: T => U): Try[U]^{this} = this.asTryOf[U]
  /** Returns this `Failure` unchanged since there is no value to apply the partial function to.
   *
   *  @tparam U the type of the value returned by the partial function
   *  @param pf the partial function to apply to the value if this were a `Success`
   *  @return this `Failure` unchanged
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{this} = this.asTryOf[U]
  /** Returns this `Failure` unchanged since there is no value to test against the predicate.
   *
   *  @param p the predicate to test the value against
   *  @return this `Failure` unchanged
   */
  override def filter(p: T => Boolean): Try[T]^{this} = this
  /** Applies the given partial function to the exception held by this `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply if this is a `Failure`
   *  @return a `Success` wrapping the result of applying `pf` to the exception if `pf` is defined for it, this `Failure` unchanged if `pf` is not defined for the exception; any non-fatal exception thrown by `pf` is caught and returned as a `Failure`
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U]^{this, pf.only[Control]} = {
    val marker = Statics.pfMarker
    try {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
      if (marker ne v.asInstanceOf[AnyRef]) Success(v.asInstanceOf[U]) else this
    } catch { case NonFatal(e) => Failure(e) }
  }
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U]^{this, pf} = {
    val marker = Statics.pfMarker
=======
      if (marker ne v.asInstanceOf[AnyRef]) Success(v.asInstanceOf[U]) else this
    } catch { case NonFatal(e) => Failure(e) }
  }
  /** Applies the given partial function to the exception held by this `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply if this is a `Failure`
   *  @return the `Try` produced by applying `pf` to the exception if `pf` is defined for it, this `Failure` unchanged if `pf` is not defined for the exception; any non-fatal exception thrown by `pf` is caught and returned as a `Failure`
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U]^{this, pf} = {
    val marker = Statics.pfMarker
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
      if (marker ne v.asInstanceOf[AnyRef]) v.asInstanceOf[Try[U]] else this
    } catch { case NonFatal(e) => Failure(e) }
  }
  /** TODO FILL IN */
  override def failed: Try[Throwable] = Success(exception)
  /** TODO FILL IN */
  override def toOption: Option[T] = None
  /** TODO FILL IN */
  override def toEither: Either[Throwable, T] = Left(exception)
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param fa TODO FILL IN
   *  @param fb TODO FILL IN
   *  @return TODO FILL IN
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U = fa(exception)

=======
      if (marker ne v.asInstanceOf[AnyRef]) v.asInstanceOf[Try[U]] else this
    } catch { case NonFatal(e) => Failure(e) }
  }
  /** Returns a `Success` containing the exception held by this `Failure`. */
  override def failed: Try[Throwable] = Success(exception)
  /** Returns `None` since this is a `Failure`. */
  override def toOption: Option[T] = None
  /** Returns `Left` with the exception held by this `Failure`. */
  override def toEither: Either[Throwable, T] = Left(exception)
  /** Applies the given function to the exception held by this `Failure`.
   *
   *  @tparam U the type of the result
   *  @param fa the function to apply to the exception
   *  @param fb the function to apply to the value if this were a `Success`
   *  @return the result of applying `fa` to the exception
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U = fa(exception)

>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
}

/** TODO FILL IN
 *
 *  @tparam T TODO FILL IN
 *  @param value TODO FILL IN
 */
final case class Success[+T](value: T) extends Try[T] {
  /** TODO FILL IN */
  override def isFailure: Boolean = false
  /** TODO FILL IN */
  override def isSuccess: Boolean = true
  /** TODO FILL IN */
  override def get = value
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def getOrElse[U >: T](default: => U): U = get
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U] = this
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{f} =
    try f(value) catch { case NonFatal(e) => Failure(e) }
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param ev TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   */
  override def foreach[U](f: T => U): Unit = f(value)
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param s TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
    val marker = Statics.pfMarker
=======
}

/** Represents a successful computation that holds a value.
 *
 *  @tparam T the type of the value held by this `Success`
 *  @param value the successfully computed value
 */
final case class Success[+T](value: T) extends Try[T] {
  /** Returns `false` since this is a `Success`. */
  override def isFailure: Boolean = false
  /** Returns `true` since this is a `Success`. */
  override def isSuccess: Boolean = true
  /** Returns the value held by this `Success`. */
  override def get = value
  /** Returns the value held by this `Success`.
   *
   *  @tparam U the type of the returned value, a supertype of `T`
   *  @param default the default value to return if this were a `Failure`
   *  @return the value held by this `Success`
   */
  override def getOrElse[U >: T](default: => U): U = get
  /** Returns this `Success` since it is already successful.
   *
   *  @tparam U the type of the value in the returned `Try`, a supertype of `T`
   *  @param default the fallback `Try` to return if this were a `Failure` (evaluated lazily)
   *  @return this `Success`
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U] = this
  /** Returns the given function applied to the value held by this `Success`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param f the function to apply to the value
   *  @return the `Try` returned by applying `f` to the value; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{f} =
    try f(value) catch { case NonFatal(e) => Failure(e) }
  /** Flattens this `Success` by returning the inner `Try[U]`.
   *
   *  @tparam U the type of the value in the inner `Try`
   *  @param ev evidence that `T` is itself a `Try[U]`
   *  @return the inner `Try[U]`
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
  /** Applies the given function to the value held by this `Success`.
   *
   *  @tparam U the (discarded) result type of the function `f`
   *  @param f the function to apply to the value
   */
  override def foreach[U](f: T => U): Unit = f(value)
  /** Applies the given functions to transform this `Success`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param s the function to apply to the value if this is a `Success`
   *  @param f the function to apply to the exception if this were a `Failure`
   *  @return the `Try` returned by applying `s` to the value; any non-fatal exception thrown by `s` is caught and returned as a `Failure`
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
  /** Maps the given function to the value held by this `Success`.
   *
   *  @tparam U the type of the mapped value
   *  @param f the function to apply to the value
   *  @return a `Success` containing the result of applying `f` to the value; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
  /** Applies the given partial function to the value held by this `Success`.
   *
   *  @tparam U the type of the value returned by the partial function
   *  @param pf the partial function to apply to the value
   *  @return a `Success` containing the result of `pf` applied to the value if `pf` is defined at it, a `Failure` with a `NoSuchElementException` if `pf` is not defined at the value; any non-fatal exception thrown by `pf` is caught and returned as a `Failure`
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
    val marker = Statics.pfMarker
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
      else Failure(new NoSuchElementException("Predicate does not hold for " + value))
    } catch { case NonFatal(e) => Failure(e) }
  }
  /** TODO FILL IN
   *
   *  @param p TODO FILL IN
   *  @return TODO FILL IN
   */
  override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
    try {
      if (p(value)) this else Failure(new NoSuchElementException("Predicate does not hold for " + value))
    } catch { case NonFatal(e) => Failure(e) }
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
  /** TODO FILL IN */
  override def failed: Try[Throwable] = Failure(new UnsupportedOperationException("Success.failed"))
  /** TODO FILL IN */
  override def toOption: Option[T] = Some(value)
  /** TODO FILL IN */
  override def toEither: Either[Throwable, T] = Right(value)
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param fa TODO FILL IN
   *  @param fb TODO FILL IN
   *  @return TODO FILL IN
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U =
    try { fb(value) } catch { case NonFatal(e) => fa(e) }
=======
      else Failure(new NoSuchElementException("Predicate does not hold for " + value))
    } catch { case NonFatal(e) => Failure(e) }
  }
  /** Converts this to a `Failure` if the predicate is not satisfied.
   *
   *  @param p the predicate to test the value against
   *  @return this `Success` if the value satisfies `p`, a `Failure` with a `NoSuchElementException` if the value does not satisfy `p`; any non-fatal exception thrown by `p` is caught and returned as a `Failure`
   */
  override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
    try {
      if (p(value)) this else Failure(new NoSuchElementException("Predicate does not hold for " + value))
    } catch { case NonFatal(e) => Failure(e) }
  /** Returns this `Success` unchanged since there is no exception to recover from.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply if this were a `Failure`
   *  @return this `Success` unchanged
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
  /** Returns this `Success` unchanged since there is no exception to recover from.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply if this were a `Failure`
   *  @return this `Success` unchanged
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
  /** Returns a `Failure` containing an `UnsupportedOperationException`. */
  override def failed: Try[Throwable] = Failure(new UnsupportedOperationException("Success.failed"))
  /** Returns `Some` containing the value held by this `Success`. */
  override def toOption: Option[T] = Some(value)
  /** Returns `Right` with the value held by this `Success`. */
  override def toEither: Either[Throwable, T] = Right(value)
  /** Applies the given function to the value held by this `Success`.
   *
   *  @tparam U the type of the result
   *  @param fa the function to apply to the exception if this were a `Failure`
   *  @param fb the function to apply to the value if this is a `Success`
   *  @return the result of applying `fb` to the value; if `fb` throws an exception, the result of applying `fa` to that exception
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U =
    try { fb(value) } catch { case NonFatal(e) => fa(e) }
>>>>>>> REPLACE
```