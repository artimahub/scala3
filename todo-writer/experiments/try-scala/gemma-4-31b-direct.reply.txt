library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
    def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
=======
    /** Maps the given function to the value if this `Try` is a `Success` and the predicate `p` is satisfied.
     *
     *  @tparam U the type of the mapped value
     *  @param f the function to apply to the value if it satisfies `p`
     *  @return a `Success` containing the result of applying `f` if this is a `Success` and `p` is satisfied, otherwise a `Failure`
     */
    def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
    def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
=======
    /** Applies the given function `f` if this `Try` is a `Success` and the predicate `p` is satisfied.
     *
     *  @tparam U the type of the value in the resulting `Try`
     *  @param f the function to apply to the value if it satisfies `p`
     *  @return the `Try` returned by `f` if this is a `Success` and `p` is satisfied, otherwise a `Failure`
     */
    def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     */
    def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
=======
    /** Applies the given function `f` if this `Try` is a `Success` and the predicate `p` is satisfied.
     *
     *  @tparam U the (discarded) result type of the function `f`
     *  @param f the function to apply to the value if it satisfies `p`
     */
    def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    /** TODO FILL IN
     *
     *  @param q TODO FILL IN
     *  @return TODO FILL IN
     */
    def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
=======
    /** Creates a non-strict filter by combining the existing predicate `p` with the given predicate `q`.
     *
     *  @param q the additional predicate to test elements against
     *  @return a `WithFilter` that applies both predicates `p` and `q`
     */
    def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
/** TODO FILL IN
 *
 *  @tparam T TODO FILL IN
 *  @param exception TODO FILL IN
 */
final case class Failure[+T](exception: Throwable) extends Try[T] { self: Failure[T]^ =>
=======
/** Represents a failed computation that resulted in the given `exception`.
 *
 *  @tparam T the type of the value that would have been produced by a successful computation
 *  @param exception the throwable that caused the failure
 */
final case class Failure[+T](exception: Throwable) extends Try[T] { self: Failure[T]^ =>
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def isFailure: Boolean = true
=======
  /** Returns `true` since this is a `Failure`. */
  override def isFailure: Boolean = true
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def isSuccess: Boolean = false
=======
  /** Returns `false` since this is a `Failure`. */
  override def isSuccess: Boolean = false
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def get: T = throw exception
=======
  /** Throws the exception held by this `Failure`. */
  override def get: T = throw exception
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def getOrElse[U >: T](default: => U): U = default
=======
  /** Returns the given `default` argument since this is a `Failure`.
   *
   *  @tparam U the type of the returned value, a supertype of `T`
   *  @param default the default value to return
   */
  override def getOrElse[U >: T](default: => U): U = default
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U]^{default} =
    try default catch { case NonFatal(e) => Failure(e) }
=======
  /** Returns the given `default` argument since this is a `Failure`.
   *
   *  @tparam U the type of the value in the returned `Try`, a supertype of `T`
   *  @param default the fallback `Try` to return (evaluated lazily)
   *  @return the `default` `Try`; any non-fatal exception thrown while evaluating `default` is caught and returned as a `Failure`
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U]^{default} =
    try default catch { case NonFatal(e) => Failure(e) }
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{this} = this.asTryOf[U]
=======
  /** Returns this `Failure` unchanged since it is not a `Success`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param f the function to apply to the value if this were a `Success`
   *  @return this `Failure` cast to `Try[U]`
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{this} = this.asTryOf[U]
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param ev TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U]^{this} = this.asTryOf[U]
=======
  /** Returns this `Failure` unchanged since it is not a `Success`.
   *
   *  @tparam U the type of the value in the inner `Try`
   *  @param ev evidence that `T` is itself a `Try[U]`
   *  @return this `Failure` cast to `Try[U]`
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U]^{this} = this.asTryOf[U]
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   */
  override def foreach[U](f: T => U): Unit = ()
=======
  /** Does nothing since this is a `Failure`.
   *
   *  @tparam U the (discarded) result type of the function `f`
   *  @param f the function to apply to the value if this were a `Success`
   */
  override def foreach[U](f: T => U): Unit = ()
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param s TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{f} =
    try f(exception) catch { case NonFatal(e) => Failure(e) }
=======
  /** Applies the function `f` to the exception held by this `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param s the function to apply if this were a `Success`
   *  @param f the function to apply to the exception
   *  @return the `Try` returned by `f` applied to the exception; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{f} =
    try f(exception) catch { case NonFatal(e) => Failure(e) }
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def map[U](f: T => U): Try[U]^{this} = this.asTryOf[U]
=======
  /** Returns this `Failure` unchanged since it is not a `Success`.
   *
   *  @tparam U the type of the mapped value
   *  @param f the function to apply to the value if this were a `Success`
   *  @return this `Failure` cast to `Try[U]`
   */
  override def map[U](f: T => U): Try[U]^{this} = this.asTryOf[U]
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{this} = this.asTryOf[U]
=======
  /** Returns this `Failure` unchanged since it is not a `Success`.
   *
   *  @tparam U the type of the value returned by the partial function
   *  @param pf the partial function to apply if this were a `Success`
   *  @return this `Failure` cast to `Try[U]`
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{this} = this.asTryOf[U]
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @param p TODO FILL IN
   *  @return TODO FILL IN
   */
  override def filter(p: T => Boolean): Try[T]^{this} = this
=======
  /** Returns this `Failure` unchanged since it is not a `Success`.
   *
   *  @param p the predicate to test the value against
   *  @return this `Failure` unchanged
   */
  override def filter(p: T => Boolean): Try[T]^{this} = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U]^{this, pf.only[Control]} = {
=======
  /** Applies the partial function `pf` to the exception held by this `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply to the exception
   *  @return a `Success` wrapping the result of applying `pf` to the exception if `pf` is defined for it, this `Failure` unchanged if `pf` is not defined for the exception, or a `Failure` if `pf` throws a non-fatal exception
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U]^{this, pf.only[Control]} = {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U]^{this, pf} = {
=======
  /** Applies the partial function `pf` to the exception held by this `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply to the exception
   *  @return the `Try` produced by applying `pf` to the exception if `pf` is defined for it, this `Failure` unchanged if `pf` is not defined for the exception, or a `Failure` if `pf` throws a non-fatal exception
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U]^{this, pf} = {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def failed: Try[Throwable] = Success(exception)
=======
  /** Returns the exception of this `Failure` wrapped in a `Success`. */
  override def failed: Try[Throwable] = Success(exception)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def toOption: Option[T] = None
=======
  /** Returns `None` since this is a `Failure`. */
  override def toOption: Option[T] = None
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def toEither: Either[Throwable, T] = Left(exception)
=======
  /** Returns a `Left` containing the exception since this is a `Failure`. */
  override def toEither: Either[Throwable, T] = Left(exception)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param fa TODO FILL IN
   *  @param fb TODO FILL IN
   *  @return TODO FILL IN
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U = fa(exception)
=======
  /** Applies the function `fa` to the exception held by this `Failure`.
   *
   *  @tparam U the type of the result
   *  @param fa the function to apply since this is a `Failure`
   *  @param fb the function to apply if this were a `Success`
   *  @return the result of applying `fa` to the exception
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U = fa(exception)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
/** TODO FILL IN
 *
 *  @tparam T TODO FILL IN
 *  @param value TODO FILL IN
 */
final case class Success[+T](value: T) extends Try[T] {
=======
/** Represents a successfully computed value.
 *
 *  @tparam T the type of the successful value
 *  @param value the computed value
 */
final case class Success[+T](value: T) extends Try[T] {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def isFailure: Boolean = false
=======
  /** Returns `false` since this is a `Success`. */
  override def isFailure: Boolean = false
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def isSuccess: Boolean = true
=======
  /** Returns `true` since this is a `Success`. */
  override def isSuccess: Boolean = true
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def get = value
=======
  /** Returns the value held by this `Success`. */
  override def get = value
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def getOrElse[U >: T](default: => U): U = get
=======
  /** Returns the value held by this `Success`.
   *
   *  @tparam U the type of the returned value, a supertype of `T`
   *  @param default the default value to return if this were a `Failure`
   */
  override def getOrElse[U >: T](default: => U): U = get
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U] = this
=======
  /** Returns this `Success` unchanged.
   *
   *  @tparam U the type of the value in the returned `Try`, a supertype of `T`
   *  @param default the fallback `Try` to return if this were a `Failure`
   *  @return this `Success`
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{f} =
    try f(value) catch { case NonFatal(e) => Failure(e) }
=======
  /** Applies the given function `f` to the value held by this `Success`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param f the function to apply to the value
   *  @return the `Try` returned by `f` applied to the value; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{f} =
    try f(value) catch { case NonFatal(e) => Failure(e) }
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param ev TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
=======
  /** Returns the inner `Try` held by this `Success`.
   *
   *  @tparam U the type of the value in the inner `Try`
   *  @param ev evidence that `T` is itself a `Try[U]`
   *  @return the inner `Try[U]`
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   */
  override def foreach[U](f: T => U): Unit = f(value)
=======
  /** Applies the given function `f` to the value held by this `Success`.
   *
   *  @tparam U the (discarded) result type of the function `f`
   *  @param f the function to apply to the value
   */
  override def foreach[U](f: T => U): Unit = f(value)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param s TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
=======
  /** Applies the function `s` to the value held by this `Success`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param s the function to apply to the value
   *  @param f the function to apply if this were a `Failure`
   *  @return the `Try` returned by `s` applied to the value; any non-fatal exception thrown by `s` is caught and returned as a `Failure`
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
=======
  /** Applies the given function `f` to the value held by this `Success`.
   *
   *  @tparam U the type of the mapped value
   *  @param f the function to apply to the value
   *  @return a `Success` containing the result of applying `f` to the value; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
   */
  override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
=======
  /** Applies the given partial function `pf` to the value held by this `Success`.
   *
   *  @tparam U the type of the value returned by the partial function
   *  @param pf the partial function to apply to the value
   *  @return a `Success` containing the result of `pf` applied to the value if `pf` is defined at it, a `Failure` with a `NoSuchElementException` if `pf` is not defined at the value, or a `Failure` if `pf` throws a non-fatal exception
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @param p TODO FILL IN
   *  @return TODO FILL IN
   */
  override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
=======
  /** Returns this `Success` if the value satisfies the predicate `p`, otherwise returns a `Failure` with a `NoSuchElementException`.
   *
   *  @param p the predicate to test the value against
   *  @return this `Success` if `p` is satisfied, a `Failure` with a `NoSuchElementException` if not, or a `Failure` if `p` throws a non-fatal exception
   */
  override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
=======
  /** Returns this `Success` unchanged since it is not a `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply if this were a `Failure`
   *  @return this `Success`
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
=======
  /** Returns this `Success` unchanged since it is not a `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`, a supertype of `T`
   *  @param pf the partial function to apply if this were a `Failure`
   *  @return this `Success`
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def failed: Try[Throwable] = Failure(new UnsupportedOperationException("Success.failed"))
=======
  /** Returns a `Failure` containing an `UnsupportedOperationException` since this is a `Success`. */
  override def failed: Try[Throwable] = Failure(new UnsupportedOperationException("Success.failed"))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def toOption: Option[T] = Some(value)
=======
  /** Returns a `Some` containing the value held by this `Success`. */
  override def toOption: Option[T] = Some(value)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def toEither: Either[Throwable, T] = Right(value)
=======
  /** Returns a `Right` containing the value held by this `Success`. */
  override def toEither: Either[Throwable, T] = Right(value)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param fa TODO FILL IN
   *  @param fb TODO FILL IN
   *  @return TODO FILL IN
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U =
=======
  /** Applies the function `fb` to the value held by this `Success`.
   *
   *  @tparam U the type of the result
   *  @param fa the function to apply if this were a `Failure`
   *  @param fb the function to apply since this is a `Success`
   *  @return the result of applying `fb` to the value, or the result of applying `fa` to any non-fatal exception thrown by `fb`
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U =
>>>>>>> REPLACE
```