library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def isFailure: Boolean = false
=======
  /** Indicates that this `Try` is a `Failure`. */
  override def isFailure: Boolean = false
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def isSuccess: Boolean = true
=======
  /** Indicates that this `Try` is a `Success`. */
  override def isSuccess: Boolean = true
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def get = value
=======
  /** Returns the value of this `Success`. */
  override def get = value
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def getOrElse[U >: T](default: => U): U = get
=======
  /** Returns the value of this `Success`, ignoring the supplied default.
   *
   *  @tparam U the type of the returned value, a supertype of `T`
   *  @param default the default value to return if this is a `Failure` (ignored for a `Success`)
   */
  override def getOrElse[U >: T](default: => U): U = get
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param default TODO FILL IN
   *  @return TODO FILL IN
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U] = this
=======
  /** Returns this `Success` unchanged, ignoring the supplied fallback `Try`.
   *
   *  @tparam U the type of the value in the returned `Try`, a supertype of `T`
   *  @param default the fallback `Try` to return if this is a `Failure` (ignored for a `Success`)
   */
  override def orElse[U >: T](default: => Try[U]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{f} =
    try f(value) catch { case NonFatal(e) => Failure(e) }
=======
  /** Applies the function `f` to the value of this `Success` and returns the resulting `Try`,
   *  catching any non‑fatal exception and converting it to a `Failure`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param f the function to apply to the value if this is a `Success`
   *  @return the `Try` returned by applying `f` to the value, or a `Failure` if `f` throws a non‑fatal exception
   */
  override def flatMap[U](f: T => Try[U]^): Try[U]^{f} =
    try f(value) catch { case NonFatal(e) => Failure(e) }
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param ev TODO FILL IN
   *  @return TODO FILL IN
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
=======
  /** Returns the inner `Try` value if this `Success` holds a nested `Try`.
   *
   *  @tparam U the type of the value in the inner `Try`
   *  @param ev evidence that the value type `T` is a `Try[U]`
   */
  override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   */
  override def foreach[U](f: T => U): Unit = f(value)
=======
  /** Applies the given function `f` to the value of this `Success`.
   *
   *  @tparam U the result type of the function `f` (ignored)
   *  @param f the function to apply to the value
   */
  override def foreach[U](f: T => U): Unit = f(value)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param s TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
=======
  /** Applies the function `s` to the value of this `Success` and returns the resulting `Try`;
   *  the function `f` is ignored because this is a `Success`.
   *
   *  @tparam U the type of the value in the resulting `Try`
   *  @param s the function to apply to the value if this is a `Success`
   *  @param f the function to apply to the exception if this were a `Failure` (ignored for a `Success`)
   *  @return the `Try` returned by `s`
   */
  override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param f TODO FILL IN
   *  @return TODO FILL IN
   */
  override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
=======
  /** Applies the function `f` to the value of this `Success` and wraps the result in a `Success`.
   *
   *  @tparam U the type of the mapped value
   *  @param f the function to apply to the value
   *  @return a `Success` containing the result of applying `f`
   */
  override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
=======
  /** Applies the partial function `pf` to the value of this `Success` and returns a `Success`
   *  with the result if `pf` is defined, otherwise returns a `Failure` with a `NoSuchElementException`.
   *
   *  @tparam U the type of the value returned by the partial function
   *  @param pf the partial function to apply to the value
   *  @return a `Success` containing the result if `pf` is defined, or a `Failure` with `NoSuchElementException` otherwise
   */
  override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @param p TODO FILL IN
   *  @return TODO FILL IN
   */
  override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
=======
  /** Tests the value of this `Success` against the predicate `p` and returns this `Success`
   *  if the predicate holds, otherwise returns a `Failure` with a `NoSuchElementException`.
   *
   *  @param p the predicate to test the value
   *  @return this `Success` if the predicate holds, otherwise a `Failure` with `NoSuchElementException`; any non‑fatal exception thrown by `p` is caught and returned as a `Failure`
   */
  override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
=======
  /** Returns this `Success` unchanged because there is no exception to recover from.
   *
   *  @tparam U the supertype of `T` that the recovered value would have
   *  @param pf the partial function to apply if this were a `Failure` (ignored for a `Success`)
   */
  override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param pf TODO FILL IN
   *  @return TODO FILL IN
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
=======
  /** Returns this `Success` unchanged because there is no exception to recover from.
   *
   *  @tparam U the supertype of `T` that the recovered `Try` would produce
   *  @param pf the partial function to apply if this were a `Failure` (ignored for a `Success`)
   */
  override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def failed: Try[Throwable] = Failure(new UnsupportedOperationException("Success.failed"))
=======
  /** Returns a `Failure` wrapping an `UnsupportedOperationException` because a `Success` cannot be inverted. */
  override def failed: Try[Throwable] = Failure(new UnsupportedOperationException("Success.failed"))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def toOption: Option[T] = Some(value)
=======
  /** Returns `Some` containing the value of this `Success`. */
  override def toOption: Option[T] = Some(value)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN */
  override def toEither: Either[Throwable, T] = Right(value)
=======
  /** Returns `Right` containing the value of this `Success`. */
  override def toEither: Either[Throwable, T] = Right(value)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
  /** TODO FILL IN
   *
   *  @tparam U TODO FILL IN
   *  @param fa TODO FILL IN
   *  @param fb TODO FILL IN
   *  @return TODO FILL IN
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U =
=======
  /** Applies `fb` to the value of this `Success` and returns the result; if `fb` throws a non‑fatal exception,
   *  applies `fa` to the exception and returns that result.
   *
   *  @tparam U the result type of the fold
   *  @param fa the function to apply to an exception (unused for a `Success`)
   *  @param fb the function to apply to the value
   *  @return the result of applying `fb` to the value, or the result of `fa` if `fb` throws a non‑fatal exception
   */
  override def fold[U](fa: Throwable => U, fb: T => U): U =
>>>>>>> REPLACE
```