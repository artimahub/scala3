library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @param p the predicate used to test elements
    */
   final class WithFilter(p: T => Boolean) uses Try.this {
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
    def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
    def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     */
    def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
    /** TODO FILL IN
     *
     *  @param q TODO FILL IN
     *  @return TODO FILL IN
     */
    def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
=======
    *  @param p the predicate used to test elements
    */
   final class WithFilter(p: T => Boolean) uses Try.this {
    /** Applies the given function `f` to those elements of this `Try` which satisfy the predicate `p`.
     *
     *  @tparam U the type of the value in the resulting `Try`
     *  @param f the function to apply to the element if the predicate is satisfied
     *  @return a `Success` containing the result of applying `f` to the element if the predicate is satisfied, or this `Failure` unchanged; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
     */
    def map[U](f:     T => U): Try[U]^{Try.this, p.only[Control], f.only[Control]} = Try.this.filter(p).map(f)
    /** Applies the given function `f` to those elements of this `Try` which satisfy the predicate `p`.
     *
     *  @tparam U the type of the value in the resulting `Try`
     *  @param f the function to apply to the element if the predicate is satisfied
     *  @return a `Success` containing the result of applying `f` to the element if the predicate is satisfied, or this `Failure` unchanged; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
     */
    def flatMap[U](f: T => Try[U]^): Try[U]^{Try.this, p.only[Control], f}         = Try.this.filter(p).flatMap(f)
    /** Applies the given function `f` to those elements of this `Try` which satisfy the predicate `p`.
     *
     *  @tparam U the (discarded) result type of the function `f`
     *  @param f the function to apply to the element if the predicate is satisfied
     */
    def foreach[U](f: T => U): Unit                                                = Try.this.filter(p).foreach(f)
    /** Returns a new `WithFilter` with the combined predicate `p && q`.
     *
     *  @param q another predicate used to test elements
     *  @return a new `WithFilter` with the combined predicate
     */
    def withFilter(q: T => Boolean): WithFilter^{Try.this, p, q}                   = new WithFilter(x => p(x) && q(x))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @param exception the exception that caused the failure
    */
   final case class Failure[+T](exception: Throwable) extends Try[T] { self: Failure[T]^ =>
    /** TODO FILL IN */
    override def isFailure: Boolean = true
    /** TODO FILL IN */
    override def isSuccess: Boolean = false
    /** TODO FILL IN */
    override def get: T = throw exception
=======
    *  @param exception the exception that caused the failure
    */
   final case class Failure[+T](exception: Throwable) extends Try[T] { self: Failure[T]^ =>
    /** Returns `true` for all `Failure` instances. */
    override def isFailure: Boolean = true
    /** Returns `false` for all `Failure` instances. */
    override def isSuccess: Boolean = false
    /** Returns the exception wrapped in this `Failure`. */
    override def get: T = throw exception
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param default TODO FILL IN
    *  @return TODO FILL IN
    */
   override def getOrElse[U >: T](default: => U): U = default
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param default TODO FILL IN
     *  @return TODO FILL IN
     */
   override def orElse[U >: T](default: => Try[U]^): Try[U]^{default} =
=======
    *  @tparam U the type of the returned value, a supertype of `T`
    *  @param default the default value to return if this is a `Failure`
    */
   override def getOrElse[U >: T](default: => U): U = default
    /** Returns the given `default` `Try` argument since this is a `Failure`.
     *
     *  @tparam U the type of the value in the returned `Try`, a supertype of `T`
     *  @param default the fallback `Try` to return if this is a `Failure` (evaluated lazily)
     */
   override def orElse[U >: T](default: => Try[U]^): Try[U]^{default} =
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param f TODO FILL IN
    *  @return TODO FILL IN
    */
   override def flatMap[U](f: T => Try[U]^): Try[U]^{this} = this.asTryOf[U]
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param ev TODO FILL IN
     *  @return TODO FILL IN
     */
   override def flatten[U](implicit ev: T <:< Try[U]): Try[U]^{this} = this.asTryOf[U]
=======
    *  @tparam U the type of the value in the resulting `Try`
    *  @param f the function to apply to the value if this is a `Success`; not used because this is a `Failure`
    *  @return this `Failure` unchanged
     */
   override def flatMap[U](f: T => Try[U]^): Try[U]^{this} = this.asTryOf[U]
    /** Returns the inner value if this is a `Success`, otherwise returns this `Failure`.
     *
     *  @tparam U the type of the value in the inner `Try`
     *  @param ev evidence that `T` is a `Try[U]`; this is a capture-checking helper and is unused in the implementation
     *  @return the inner `Try[U]` if this is a `Success`, or this `Failure` unchanged
     */
   override def flatten[U](implicit ev: T <:< Try[U]): Try[U]^{this} = this.asTryOf[U]
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param s TODO FILL IN
    *  @param f TODO FILL IN
    *  @return TODO FILL IN
    */
   override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{f} =
     try f(exception) catch { case NonFatal(e) => Failure(e) }
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
   override def map[U](f: T => U): Try[U]^{this} = this.asTryOf[U]
=======
    *  @tparam U the type of the value in the resulting `Try`
     *  @param s the function to apply if this is a `Success`; not used because this is a `Failure`
     *  @param f the function to apply to the exception if this is a `Failure`
     *  @return the `Try` returned by `f` applied to the exception if this is a `Failure`; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
     */
   override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{f} =
     try f(exception) catch { case NonFatal(e) => Failure(e) }
    /** Returns a `Success` wrapping the given function's result, since this is a `Failure`.
     *
     *  @tparam U the type of the mapped value
     *  @param f the function to apply to the value; not used because this is a `Failure`
     *  @return a `Success` wrapping the given function's result
     */
   override def map[U](f: T => U): Try[U]^{this} = this.asTryOf[U]
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param pf TODO FILL IN
    *  @return TODO FILL IN
    */
   override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{this} = this.asTryOf[U]
    /** TODO FILL IN
     *
     *  @param p TODO FILL IN
     *  @return TODO FILL IN
     */
   override def filter(p: T => Boolean): Try[T]^{this} = this
=======
    *  @tparam U the type of the value returned by the partial function; not used because this is a `Failure`
     *  @param pf the partial function to apply to the value; not used because this is a `Failure`
     *  @return this `Failure` unchanged
     */
   override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{this} = this.asTryOf[U]
    /** Returns this `Try` unchanged.
     *
     *  @param p the predicate to test the value against; not used because this is a `Failure`
     *  @return this `Try` unchanged
     */
   override def filter(p: T => Boolean): Try[T]^{this} = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param pf TODO FILL IN
    *  @return TODO FILL IN
    */
   override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U]^{this, pf.only[Control]} = {
=======
    *  @tparam U the type of the value in the resulting `Try`
     *  @param pf the partial function to apply if this is a `Failure`
     *  @return a `Success` wrapping the result of applying `pf` to the exception if this is a `Failure` and `pf` is defined for it, or this `Failure` unchanged if `pf` is not defined for the exception
     */
   override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U]^{this, pf.only[Control]} = {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param pf TODO FILL IN
    *  @return TODO FILL IN
    */
   override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U]^{this, pf} = {
=======
    *  @tparam U the type of the value in the resulting `Try`
     *  @param pf the partial function to apply if this is a `Failure`
     *  @return the `Try` produced by applying `pf` to the exception if this is a `Failure` and `pf` is defined for it, or this `Failure` unchanged if `pf` is not defined for the exception
     */
   override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U]^{this, pf} = {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param fa TODO FILL IN
    *  @param fb TODO FILL IN
    *  @return TODO FILL IN
    */
   override def fold[U](fa: Throwable => U, fb: T => U): U = fa(exception)

   private inline def asTryOf[U]: Try[U]^{this} = this.asInstanceOf[Try[U]]
=======
    *  @tparam U the type of the result
     *  @param fa the function to apply if this is a `Failure`
     *  @param fb the function to apply if this is a `Success`; not used because this is a `Failure`
     *  @return the result of applying `fa` to the exception
     */
   override def fold[U](fa: Throwable => U, fb: T => U): U = fa(exception)

   private inline def asTryOf[U]: Try[U]^{this} = this.asInstanceOf[Try[U]]
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam T the type of the value computed by the `Try`
    *  @param value the successfully computed value
    */
   final case class Success[+T](value: T) extends Try[T] {
    /** TODO FILL IN */
    override def isFailure: Boolean = false
    /** TODO FILL IN */
    override def isSuccess: Boolean = true
    /** TODO FILL IN */
    override def get = value
=======
    *  @tparam T the type of the value computed by the `Try`
    *  @param value the successfully computed value
    */
   final case class Success[+T](value: T) extends Try[T] {
    /** Returns `false` for all `Success` instances. */
    override def isFailure: Boolean = false
    /** Returns `true` for all `Success` instances. */
    override def isSuccess: Boolean = true
    /** Returns the successfully computed value. */
    override def get = value
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param default TODO FILL IN
    *  @return TODO FILL IN
    */
   override def getOrElse[U >: T](default: => U): U = get
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param default TODO FILL IN
     *  @return TODO FILL IN
     */
   override def orElse[U >: T](default: => Try[U]^): Try[U] = this
=======
    *  @tparam U the type of the returned value, a supertype of `T`
     *  @param default the default value to return if this is a `Failure`; not used because this is a `Success`
     *  @return the successfully computed value
     */
   override def getOrElse[U >: T](default: => U): U = get
    /** Returns this `Try` if it is a `Success`, or the given `default` `Try` argument if this is a `Failure`.
     *
     *  @tparam U the type of the value in the returned `Try`, a supertype of `T`
     *  @param default the fallback `Try` to return if this is a `Failure` (evaluated lazily)
     */
   override def orElse[U >: T](default: => Try[U]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param ev TODO FILL IN
    *  @return TODO FILL IN
    */
   override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
   override def foreach[U](f: T => U): Unit = f(value)
=======
    *  @tparam U the type of the value in the resulting `Try`; this is a capture-checking helper and is unused in the implementation
     *  @param ev evidence that `T` is a `Try[U]`; not used because this is a `Success` instance, so `T` is the value itself
     *  @return this `Success` unchanged
     */
   override def flatten[U](implicit ev: T <:< Try[U]): Try[U] = value
    /** Applies the given function `f` to the value.
     *
     *  @tparam U the (discarded) result type of the function `f`
     *  @param f the function to apply to the value
     */
   override def foreach[U](f: T => U): Unit = f(value)
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param s TODO FILL IN
    *  @param f TODO FILL IN
    *  @return TODO FILL IN
    */
   override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param f TODO FILL IN
     *  @return TODO FILL IN
     */
   override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
=======
    *  @tparam U the type of the value in the resulting `Try`
     *  @param s the function to apply if this is a `Success`
     *  @param f the function to apply if this is a `Failure`; not used because this is a `Success`
     *  @return the `Try` returned by `s` applied to the value; any non-fatal exception thrown by `s` is caught and returned as a `Failure`
     */
   override def transform[U](s: T => Try[U]^, f: Throwable => Try[U]^): Try[U]^{s} = this flatMap s
    /** Returns a `Success` wrapping the result of applying the given function `f` to the value.
     *
     *  @tparam U the type of the mapped value
     *  @param f the function to apply to the value
     *  @return a `Success` containing the result of applying `f` to the value; any non-fatal exception thrown by `f` is caught and returned as a `Failure`
     */
   override def map[U](f: T => U): Try[U]^{f.only[Control]} = Try[U](f(value))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param pf TODO FILL IN
    *  @return TODO FILL IN
    */
   override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
=======
    *  @tparam U the type of the value returned by the partial function
     *  @param pf the partial function to apply to the value
     *  @return a `Success` containing the result of `pf` applied to the value if `pf` is defined at it, a `Failure` with a `NoSuchElementException` if `pf` is not defined at the value
     */
   override def collect[U](pf: PartialFunction[T, U]^): Try[U]^{pf.only[Control]} = {
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @param p TODO FILL IN
    *  @return TODO FILL IN
    */
   override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
     try {
       if (p(value)) this else Failure(new NoSuchElementException("Predicate does not hold for " + value))
=======
    *  @param p the predicate to test the value against
    *  @return this `Success` if the value satisfies the predicate, otherwise a `Failure` with a `NoSuchElementException`
     */
   override def filter(p: T => Boolean): Try[T]^{p.only[Control]} =
     try {
       if (p(value)) this else Failure(new NoSuchElementException("Predicate does not hold for " + value))
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param pf TODO FILL IN
    *  @return TODO FILL IN
    */
   override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
    /** TODO FILL IN
     *
     *  @tparam U TODO FILL IN
     *  @param pf TODO FILL IN
     *  @return TODO FILL IN
     */
   override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
=======
    *  @tparam U the type of the value in the resulting `Try`; this is a capture-checking helper and is unused in the implementation
     *  @param pf the partial function to apply if this is a `Failure`; not used because this is a `Success`
     *  @return this `Success` unchanged
     */
   override def recover[U >: T](pf: PartialFunction[Throwable, U]^): Try[U] = this
    *  @tparam U the type of the value in the resulting `Try`; this is a capture-checking helper and is unused in the implementation
     *  @param pf the partial function to apply if this is a `Failure`; not used because this is a `Success`
     *  @return this `Success` unchanged
     */
   override def recoverWith[U >: T](pf: PartialFunction[Throwable, Try[U]^]^): Try[U] = this
>>>>>>> REPLACE
```

library/src/scala/util/Try.scala
```scala
<<<<<<< SEARCH
    *  @tparam U TODO FILL IN
    *  @param fa TODO FILL IN
    *  @param fb TODO FILL IN
    *  @return TODO FILL IN
    */
   override def fold[U](fa: Throwable => U, fb: T => U): U =
     try { fb(value) } catch { case NonFatal(e) => fa(e) }
=======
    *  @tparam U the type of the result
     *  @param fa the function to apply if this is a `Failure`; not used because this is a `Success`
     *  @param fb the function to apply if this is a `Success`
     *  @return the result of applying `fb` to the value; any non-fatal exception thrown by `fb` is caught and returned by `fa`
     */
   override def fold[U](fa: Throwable => U, fb: T => U): U =
     try { fb(value) } catch { case NonFatal(e) => fa(e) }
>>>>>>> REPLACE
```